<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto"
      xmlns:tools="http://schemas.android.com/tools"
      >

<!--    <item
        android:id="@+id/action_delete"
        android:icon="@drawable/ic_delete_normal"
        app:showAsAction="never"
        android:title="@string/delete"/>-->
<!--    android:icon="@mipmap/ic_list_white_24dp"-->
    <item
        android:id="@+id/action_config_list"
        android:icon="@mipmap/ic_list_white_24dp"
        app:showAsAction="always"
        android:visible="false"
        android:title="@string/switch_config"/>


    <item
        android:id="@+id/action_config_grid"
        android:icon="@mipmap/ic_apps_white_24dp"
        app:showAsAction="always"
        android:title="@string/switch_config"/>

</menu>